# Environment Variables Configuration
# Copy this to .env.local for local development. Never commit sensitive keys to git.

# Database URL for Prisma (default local SQLite file or Prisma Postgres connection string)
DATABASE_URL="file:./dev.db"

# Base Currency (Default: EUR)
NEXT_PUBLIC_DEFAULT_CURRENCY="EUR"
